import 'package:calibraciones/models/_usuario.dart';

class UsuarioControlador {

  Future<bool> registraUsuario(Usuario usuario) async {
    bool registroCorrecto = false;

    return registroCorrecto;
  }

  Future<Usuario?> obtenerUsuario(String? correoElectronico) async {
    Usuario? login; // Mueve la declaración de `obras` fuera del bloque `try`
    return login;
  }

  Future<bool> borrarUsuario(String? email) async {
    bool cuentaBorrada = false;
    return cuentaBorrada;
  }

  Future<bool> actualizarUsuario(Usuario usuario) async {
    bool registroCorrecto = false;

    return registroCorrecto;
  }
}
